import React from 'react';

const StepUserame = ({onNext}) => {
  return (
    <>
      <div>
        user name 
      </div>
      <button onClick={onNext}>next</button>
    </>
  );
};

export default StepUserame;